# Auth-server

Start without the --broader-- auth (patron superuser admin)

This is the server running as a daemon.

It's a centralized service for web authentication.

Multiple projects on local or remote servers? Offloads authentication for each
project with an "organization" approach.

